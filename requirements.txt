# =============================================================================
# RAG Application Requirements (Optimized - No Duplicates)
# =============================================================================

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
aiohttp==3.9.1
python-dotenv==1.0.0
requests>=2.31.0

# Data processing (numpy<2.0 for faiss compatibility)
pydantic>=2.6.0
numpy>=1.24.4,<2.0

# Vector search and embeddings
faiss-cpu==1.7.4
sentence-transformers==2.5.1

# Adapters for SPECTER2 models
adapters==1.2.0
adapter-transformers>=4.0.0

# Local LLM inference (for llama.cpp backend)
llama-cpp-python>=0.3.16

# =============================================================================
# PyTorch + CUDA (Large packages - installed first for better caching)
# =============================================================================
torch>=2.0.0
torchvision>=0.15.0

# =============================================================================
# Transformers & Image Generation
# =============================================================================
transformers>=4.40.0
accelerate>=0.20.0
diffusers>=0.27.0
pillow>=10.0.0
compel>=2.2.1

# HuggingFace ecosystem
huggingface-hub[hf-xet]>=0.35.3
peft>=0.17.1
protobuf>=6.32.1
sentencepiece>=0.2.1
propcache==0.4.1

# Progress bars
tqdm>=4.66.0

# =============================================================================
# Document Processing & OCR
# =============================================================================
docling>=2.60.0
docling-core[chunking]>=2.0.0
# tesserocr is required for Docling's TesseractOcrOptions (needs libtesseract-dev)
tesserocr>=2.6.0
pytesseract
onnxruntime

# PyMuPDF for robust PDF text extraction (fallback when OCR fails)
pymupdf>=1.24.0

# Enhanced web scraping for URL handling
httpx[http2]>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# =============================================================================
# LLM API Client (for vLLM backend)
# =============================================================================
openai>=1.0.0

# =============================================================================
# Agentic RAG Framework (LangGraph, LangChain, DSPy)
# =============================================================================
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0
dspy-ai>=2.5.0
